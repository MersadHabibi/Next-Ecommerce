export enum USER_ROLE {
  ADMIN = "ADMIN",
  USER = "USER",
}

export enum ORDER_STATUS {
  CANCELED = "Canceled",
  PROGRESS = "In progress",
  REJECTED = "Rejected",
  SENDING = "Sending",
  COMPLETED = "Completed",
}
